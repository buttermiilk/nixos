# Neuro-sama cursor assets

`theme/` is the deployable X cursor theme used by Home Manager. Cursor aliases
inside `theme/cursors/` must remain relative symlinks so the theme works from any
checkout path and after being copied into the Nix store.
